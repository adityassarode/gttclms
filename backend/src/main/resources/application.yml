server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:gttc;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: ""
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 4MB
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SUPABASE_ISSUER_URI:https://rtfxnjhmvzpwaknakwcj.supabase.co/auth/v1}

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-this-jwt-secret-in-env}
    issuer: gttc-lms
    expiryMinutes: 480
  frontendUrl: ${APP_FRONTEND_URL:http://localhost:3000}
  frontendUrls: ${APP_FRONTEND_URLS:}
  admin:
    username: ${APP_ADMIN_USERNAME:admin}
    password: ${APP_ADMIN_PASSWORD:}
  google:
    clientId: ${APP_GOOGLE_CLIENT_ID:}
    allowTestToken: ${APP_GOOGLE_ALLOW_TEST_TOKEN:false}
  mail:
    mode: ${APP_MAIL_MODE:console}
    from: ${APP_MAIL_FROM:gttc-library@gttc.local}
  storage:
    uploadDir: ${APP_UPLOAD_DIR:uploads}
