Good. Now you are at the **real integration step**.
I’ll give you exact steps. No confusion.

---

# 🔥 STEP 1: FRONTEND → SUPABASE (AUTH)

## Create supabase client

```js
// src/lib/supabase.js

import { createClient } from "@supabase/supabase-js";

export const supabase = createClient(
  "https://rtfxnjhmvzpwaknakwcj.supabase.co",
  "sb_publishable_IQWnMJQmZwLH5u6_r0hPwg_FMJCVruN"
);
```

---

## Google login

```js
await supabase.auth.signInWithOAuth({
  provider: "google"
});
```

---

## Get session (VERY IMPORTANT)

```js
const { data } = await supabase.auth.getSession();
const token = data.session?.access_token;
```

---

# 🔥 STEP 2: FRONTEND → BACKEND

Send token in every protected request:

```js
await fetch("https://gttclms.onrender.com/api/borrow", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    Authorization: `Bearer ${token}`
  },
  body: JSON.stringify({
    bookId: 1
  })
});
```

---

# 🔥 STEP 3: BACKEND → VERIFY USER (CRITICAL)

## In Spring Boot (Controller or Filter)

```java
String authHeader = request.getHeader("Authorization");

if (authHeader == null || !authHeader.startsWith("Bearer ")) {
    throw new RuntimeException("Unauthorized");
}

String token = authHeader.replace("Bearer ", "");
```

---

## Call Supabase to verify user

```java
HttpURLConnection conn = (HttpURLConnection)
    new URL("https://rtfxnjhmvzpwaknakwcj.supabase.co/auth/v1/user")
    .openConnection();

conn.setRequestProperty("Authorization", "Bearer " + token);
conn.setRequestProperty("apikey", "sb_publishable_IQWnMJQmZwLH5u6_r0hPwg_FMJCVruN");
conn.setRequestMethod("GET");

int status = conn.getResponseCode();

if (status != 200) {
    throw new RuntimeException("Invalid token");
}
```

---

# 🔥 STEP 4: BACKEND → DATABASE

## application.properties

```properties
spring.datasource.url=jdbc:postgresql://db.rtfxnjhmvzpwaknakwcj.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=YOUR_DB_PASSWORD
```

---

# 🔥 STEP 5: EMAIL (Spring Boot)

## Add dependency

```xml
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-mail</artifactId>
</dependency>
```

---

## application.properties

```properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gttclmss@gmail.com
spring.mail.password=YOUR_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
```

---

## Send HTML email

```java
@Autowired
JavaMailSender mailSender;

public void sendEmail(String to) throws Exception {
    MimeMessage message = mailSender.createMimeMessage();
    MimeMessageHelper helper = new MimeMessageHelper(message, true);

    helper.setTo(to);
    helper.setSubject("Book Borrowed");
    helper.setText("<h2>Book issued successfully</h2>", true);

    mailSender.send(message);
}
```

---

# 🔥 FINAL FLOW (IMPORTANT)

1. User clicks login
2. Supabase logs in
3. Frontend gets token
4. Frontend sends token to backend
5. Backend verifies token
6. Backend performs DB actions
7. Backend sends email

---

# ❌ COMMON MISTAKES

* Not sending token ❌
* Not verifying token ❌
* Using DB password in frontend ❌
* Calling localhost in production ❌

---


